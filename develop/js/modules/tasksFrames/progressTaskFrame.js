export function progressTaskFrame(container) {
	const progressTaskFrame = document.createElement('div');
	progressTaskFrame.className = 'progress-task-frame task-frame';
	container.append(progressTaskFrame);
}